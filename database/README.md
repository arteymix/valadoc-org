# valadoc-org database

This directory is dedicated to creating database information. Simply configure
database variables in `database.php` and run `make generate-all` to populate.
Enjoy your burrito.
